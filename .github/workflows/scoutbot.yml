name: ScoutBot — Scrape (Every 6 Hours)

# Scrapes all sources and removes expired entries every 6 hours.
# Does NOT send the email digest — that runs separately on Sundays only.
# Running every 6 hours means new opportunities appear in the sheet
# within 6 hours of being posted on the source site.
#
# 4 runs/day × ~15 min each = ~60 min/day → well within GitHub Actions free tier.

on:
  schedule:
    - cron: '0 3,9,15,21 * * *'   # 04:00, 10:00, 16:00, 22:00 WAT
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Restore Google service account JSON
        run: |
          echo "${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON_B64 }}" | base64 -d > service_account.json

      - name: Build .env file from secrets
        run: |
          cat > .env <<EOF
          SENDER_EMAIL=${{ secrets.SENDER_EMAIL }}
          GMAIL_APP_PASSWORD=${{ secrets.GMAIL_APP_PASSWORD }}
          SPREADSHEET_ID=${{ secrets.SPREADSHEET_ID }}
          FORM_SHEET_ID=1dFcnVvQjWkuYhN1rplICTY0j88KgvGqQ3FzYId2ru4s
          GOOGLE_SERVICE_ACCOUNT_JSON=service_account.json
          RECIPIENT_EMAILS=${{ secrets.RECIPIENT_EMAILS }}
          EOF

      - name: Scrape new opportunities
        run: python run.py --scrape

      - name: Clean expired entries (21-day rule)
        run: python run.py --cleanup

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: scrape-logs-${{ github.run_number }}
          path: |
            scoutbot.log
            scrapy.log
          if-no-files-found: ignore
          retention-days: 3
